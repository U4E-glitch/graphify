<resources>
    <!-- Compose draws everything; this is only the window while it starts. -->
    <style name="Theme.MailSearch" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/ic_launcher_background</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
